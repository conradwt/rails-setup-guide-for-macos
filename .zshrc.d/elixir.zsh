# set environment variables for Elixir.
# export ELIXIR_HOME=${HOME}/elixir.dir
# export ELIXIR_PROJECT=${ELIXIR_HOME}/projects/elixir
# export PATH=${ELIXIR_PROJECT}/bin:${PATH}
# export PATH=${ELIXIR_PROJECT}:${PATH}

# set environment variables to enable shell history.
export ERL_AFLAGS="-kernel shell_history enabled"
